<footer class="row foot">
    <div class="col-md-12">
      <div class="container">
        <% if session[:login] == 1 %>
        <%= link_to 'New Product', new_item_path, :class =>"btn btn-primary", :role=>"button" %>
        <%= link_to 'New Category', new_category_path, :class => "btn btn-primary", :role=>"button" %>
        <% end %>
        
         <% if user_signed_in? %>
        <%= link_to "ViewOrders", orders_path, :class => "btn btn-primary", :role=>"button" %>
          <% end %>
      </div>
    </div>
</footer>